# Produkčný build (npm run build / deploy) bez source máp (.map): na GitHub Pages
# sa nezverejňuje čitateľný zdroják a deploy je menší.
# Žiadne tajomstvá sem nepatria — kľúče sú v .env.local (gitignored).
GENERATE_SOURCEMAP=false
